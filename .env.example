# Backend API URL
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
API_BASE_URL=http://localhost:8000

# Google Sign-In: the Web OAuth client ID (from Google Cloud Console). Inlined into the client
# bundle at build time — must also be passed as a Docker build arg in production (see Dockerfile).
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
